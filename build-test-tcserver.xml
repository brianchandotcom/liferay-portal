<?xml version="1.0"?>

<project name="portal-test-tcserver" basedir="." default="test" xmlns:antelope="antlib:ise.antelope.tasks">
	<import file="build-test.xml" />

	<target name="run-selenium-tcserver">
		<if>
			<not>
				<isset property="build.app.server" />
			</not>
			<then>
				<ant antfile="build-dist.xml" target="build-dist-tcserver" />
			</then>
		</if>

		<antcall target="revert-test-properties" />

		<antcall target="prepare-virtual-host-name-properties" />

		<antcall target="start-selenium" />

		<antcall target="run-tcserver">
			<param name="test.class" value="${test.name}" />
		</antcall>

		<antcall target="stop-selenium" />
	</target>

	<target name="run-tcserver">
		<antcall target="clean-up-logs" />

		<record action="start" name="log" />

		<antcall target="run-simple-server">
			<param name="simple.server.type" value="tcserver" />
			<param name="simple.server.dir" value="${app.server.tcserver.dir}" />
			<param name="simple.server.bin.dir" value="${app.server.tcserver.bin.dir}" />
			<param name="simple.server.deploy.dir" value="${app.server.tcserver.deploy.dir}" />
			<param name="simple.server.lib.global.dir" value="${app.server.tcserver.lib.global.dir}" />
			<param name="simple.server.portal.dir" value="${app.server.tcserver.portal.dir}" />
			<param name="simple.server.start.executable" value="startup${file.suffix.bat}" />
			<param name="simple.server.start.executable.arg.line" value="" />
			<param name="simple.server.stop.executable" value="shutdown${file.suffix.bat}" />
			<param name="simple.server.stop.executable.arg.line" value="" />
		</antcall>

		<record action="stop" name="log" />

		<antcall target="evaluate-logs" />
	</target>
</project>