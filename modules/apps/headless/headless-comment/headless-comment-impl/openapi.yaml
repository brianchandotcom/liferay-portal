components:
  schemas:
    Comment:
      description: https://www.schema.org/Comment
      properties:
        comments:
          $ref: "#/components/schemas/Comment"
        creator:
          $ref: ../../headless-user-account/headless-user-account-impl/openapi.yaml/components/schemas/UserAccount
        id:
          type: integer
        self:
          type: string
        text:
          type: string
      type: object
info:
  description: ""
  title: ""
  version: 1.0.0
openapi: 3.0.1
paths:
  "/blog-posting/{parent-id}/comment":
    get:
      parameters:
        - in: query
          name: page
          schema:
            type: integer
        - in: query
          name: per_page
          schema:
            type: integer
        - in: path
          name: parent-id
          required: true
          schema:
            $ref: ../../headless-blog-posting/headless-blog-posting-impl/openapi.yaml/components/schemas/BlogPosting
        - in: query
          name: permissionchecker
          schema:
            type: string
      responses:
        200:
          content:
            "*/*":
              schema:
                items:
                  $ref: "#/components/schemas/Comment"
                type: array
          description: ""
  "/comment/{id}":
    get:
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
        - in: query
          name: permissionchecker
          schema:
            type: string
      responses:
        200:
          content:
            "*/*":
              schema:
                $ref: "#/components/schemas/Comment"
          description: ""
  "/comment/{parent-id}/comment":
    get:
      parameters:
        - in: query
          name: page
          schema:
            type: integer
        - in: query
          name: per_page
          schema:
            type: integer
        - in: path
          name: parent-id
          required: true
          schema:
            $ref: "#/components/schemas/Comment"
        - in: query
          name: permissionchecker
          schema:
            type: string
      responses:
        200:
          content:
            "*/*":
              schema:
                items:
                  $ref: "#/components/schemas/Comment"
                type: array
          description: ""
  "/document/{parent-id}/comment":
    get:
      parameters:
        - in: query
          name: page
          schema:
            type: integer
        - in: query
          name: per_page
          schema:
            type: integer
        - in: path
          name: parent-id
          required: true
          schema:
            $ref: ../../headless-document/headless-document-impl/openapi.yaml/components/schemas/Document
        - in: query
          name: permissionchecker
          schema:
            type: string
      responses:
        200:
          content:
            "*/*":
              schema:
                items:
                  $ref: "#/components/schemas/Comment"
                type: array
          description: ""
  "/structured-contents/{parent-id}/comment":
    get:
      parameters:
        - in: query
          name: page
          schema:
            type: integer
        - in: query
          name: per_page
          schema:
            type: integer
        - in: path
          name: parent-id
          required: true
          schema:
            $ref: ../../headless-structured-content/headless-structured-content-impl/openapi.yaml/components/schemas/StructuredContent
        - in: query
          name: permissionchecker
          schema:
            type: string
      responses:
        200:
          content:
            "*/*":
              schema:
                items:
                  $ref: "#/components/schemas/Comment"
                type: array
          description: ""