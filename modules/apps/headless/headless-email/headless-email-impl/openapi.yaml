components:
  schemas:
    Email:
      description: https://www.schema.org/Email
      properties:
        email:
          type: string
        id:
          type: integer
        self:
          type: string
        type:
          type: string
      type: object
info:
  description: ""
  title: ""
  version: 1.0.0
openapi: 3.0.1
paths:
  "/emails":
    get:
      parameters:
        - in: query
          name: page
          schema:
            type: integer
        - in: query
          name: per_page
          schema:
            type: integer
        - in: path
          name: genericparentid
          required: true
          schema:
            type: object
      responses:
        200:
          content:
            "*/*":
              schema:
                items:
                  $ref: "#/components/schemas/Email"
                type: array
          description: ""
  "/emails/{id}":
    get:
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        200:
          content:
            "*/*":
              schema:
                $ref: "#/components/schemas/Email"
          description: ""