components:
  schemas:
    Phone:
      description: https://www.schema.org/Phone
      properties:
        extension:
          type: string
        id:
          type: integer
        phoneNumber:
          type: string
        phoneType:
          type: string
        self:
          type: string
      type: object
info:
  description: ""
  title: ""
  version: 1.0.0
openapi: 3.0.1
paths:
  "/phones":
    get:
      parameters:
        - in: query
          name: page
          schema:
            type: integer
        - in: query
          name: per_page
          schema:
            type: integer
        - in: path
          name: genericparentid
          required: true
          schema:
            type: object
      responses:
        200:
          content:
            "*/*":
              schema:
                items:
                  $ref: "#/components/schemas/Phone"
                type: array
          description: ""
  "/phones/{id}":
    get:
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        200:
          content:
            "*/*":
              schema:
                $ref: "#/components/schemas/Phone"
          description: ""