@default = "assertElementFocused"
definition {

	function assertElementFocused {
		selenium.assertElementFocused();

		selenium.assertJavaScriptErrors("true");

		selenium.assertLiferayErrors();
	}

}