@default = "assertTextMatches"
definition {

	function assertTextMatches {
		WaitForSPARefresh();

		selenium.waitForVisible();

		selenium.assertTextMatches();

		selenium.assertJavaScriptErrors("true");

		selenium.assertLiferayErrors();
	}

}