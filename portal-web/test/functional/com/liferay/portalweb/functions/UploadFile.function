@default = "uploadFile"
definition {

	function uploadFile {
		WaitForSPARefresh();

		selenium.waitForElementPresent();

		selenium.uploadFile();

		selenium.assertJavaScriptErrors("true");

		selenium.assertLiferayErrors();
	}

}