Copy exported design tokens to `exported-design-tokens.json`.

Modify `scripts/generateTokens.ts` to match the format of your exported design tokens.

Deploy the sample or run `npm run generateTokens` to generate the `frontend-token-definition.json` and `_exported-design-tokens.scss` files.